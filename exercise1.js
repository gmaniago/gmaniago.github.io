// Define a function max() that takes two numbers as arguments and 
// returns the largest of them. Use the if-then-else construct available 
// in Javascript.

function max(a, b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

// Define a new function named max that takes two parameters
// If the first parameter has a value greater than the second parameter
// Then this function will return the first parameter
// Close the ‘if’ clause
// Otherwise (meaning if second parameter is greater than first)
// Then this function will return the second parameter
// Close the ‘else’ clause
// Close the function definition
